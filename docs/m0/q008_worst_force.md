# Q-008 — Q24.40 最坏力推导(UNSOURCED 清零)

**登记**:spec §14.1 Q-008;本文给出推导与门控常数的来源。单位 kJ/mol/nm。

## 1. 格式界

Q24.40 填满 int64:1 符号 + 23 整数 + 40 小数。物理范围 ±2²³ = **±8 388 608**。

## 2. 单对最坏力(参数包络内)

ingest 包络(spec §12 G3):|q| ≤ 1.5 e,σ ≤ 0.5 nm,ε ≤ 5.0 kJ/mol。

**LJ 排斥主导**(库仑项见下):F(r) ≈ 48ε(σ/r)¹²/r(近距近似)。

| r (nm) | (σ/r)¹² | F = 48·5·(σ/r)¹²/r |
|---|---|---|
| 0.20 | 1.49e8 | 1.79e10 |
| 0.25 | 4.10e3 | 3.93e6 |
| 0.28 | 1.05e3 | 9.02e5 |
| 0.30 | 4.33e2 | 3.46e5 |
| 0.35 | 8.82e1 | 6.06e4 |

**库仑最坏**(|q_iq_j| = 2.25 e²,直空间 erfc(αr) ≤ 1):F = KE·2.25/r²:
r = 0.25 → 5.0e3;r = 0.30 → 3.5e3。比 LJ 小三个量级,不主导。

## 3. 许用界与叠加因子

- 格式界 8.39e6,安全因子 2 → 每原子许用 **4.19e6**
- 最坏同向叠加:最近邻密堆 **12** 个邻居 → 单对许用 4.19e6/12 = **3.5e5**
- 由上表:**r_min = 0.30 nm** 时单对 LJ 力 = 3.46e5 ≤ 3.5e5 ✓

## 4. 结论(条件性成立的知识,如实登记)

**Q24.40 不溢出的条件**:非键最近距离 ≥ 0.30 nm(参数包络内)。动力学中
氢键水合壳层不会自发产生 < 0.25 nm 的非键接触;但:

1. **未最小化输入**是常态(雷区:ingest 前置条件)。→ G3b 门控:
   `MAX_INITIAL_FORCE = 2.6e4`(工程阈值:典型热力 ~1e3–1e4 的 2.6×,
   初始构象超过即要求先最小化;距格式界两个量级,留出动力学余量)
2. **运行时最终防线**:饱和(非回绕)+ 粘滞标志,每次窗口边界检查,
   **release 保留**(B10;实测:`test_saturation_sticky_fires` — 1e-4 nm
   重叠触发 LJ 力 ~1e47,饱和于 2²³ 且 sticky=True,无回绕)
3. 软核力上限(λ→0,被 decouple 的原子靠近环境原子)是 M1 的补充推导,
   此处登记为待办(软核 α=0.5 使 r→0 力有界:U_sc ~ 4ε/α²(1-λ)² 发散
   弱于 r⁻¹²,最坏力包络不大于 LJ 情形——M1 时定量复核)

## 5. 门控常数

```
MAX_ABS_Q     = 1.5   e       (G3a 包络)
MAX_SIGMA     = 0.5   nm      (G3a 包络)
MAX_EPSILON   = 5.0   kJ/mol  (G3a 包络)
MAX_INITIAL_FORCE = 2.6e4 kJ/mol/nm  (G3b,推导见上)
```
